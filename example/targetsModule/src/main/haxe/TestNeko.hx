//import tjson.TJSON;

class TestNeko {
    static function main() {
        (new Printer()).print("NEKO!!!!!!!!!!!!!!!!!!!");

        //trace("TJSON test:\n" + TJSON.encode({test : "qwe"}));
    }
}