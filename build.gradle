apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {
    compile gradleApi()
    compile localGroovy()

    compile group: 'commons-io', name: 'commons-io', version: '2.0.1'
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.1'
}